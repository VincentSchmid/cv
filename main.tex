\documentclass{article}
\usepackage{fontspec} % custom fonts
\usepackage{graphicx} % images
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{tikz} % language circles and dotted lines
\usepackage{xcolor} % define colors
\usepackage{setspace}
\usepackage{enumitem} % spacing for list items
\usepackage{fontawesome} % Icons
\usepackage{tabularx}
\usepackage{etoolbox}
\usepackage[a4paper,left=1cm, right=1cm, top=1cm, bottom=1cm]{geometry}

% Define Fonts
\setmainfont[
    Path = ./assets/fonts/Roboto/,  % adjust this path to match your setup
    Extension = .ttf,
    UprightFont = *-Regular,
    ItalicFont = *-Italic,
    BoldFont = *-Bold,
    BoldItalicFont = *-BoldItalic,
]{Roboto}
\newfontfamily\sectionfont[Path=./assets/fonts/]{Panton-BlackCaps.otf}
\newfontfamily\subsectionfont[Path=./assets/fonts/]{Acumin-BdPro.otf}

% Define Colors
\definecolor{altColor}{RGB}{0,157,255}
\definecolor{lightgray}{gray}{0.5}
\definecolor{textgray}{gray}{0.3}

% Redefine the section formats
\titleformat{\section}
{\Large\sectionfont\color{black}}
{}
{0pt}
{}
[{\titlerule[1.2pt]}] % Add a line after the section title

% Redefine the subsection format to use \subsectionfont
\titleformat{\subsection}
  {\subsectionfont\fontsize{12}{10}\selectfont\bfseries\color{black}} % Modify the font settings here
  {} % Label format
  {0pt} % Label separation
  {} % Before code
  [] % After code

% Define the altSection command
\newcommand{\altSection}[1]{%
  {\large\sectionfont\color{altColor} #1\par}%
}

\AtBeginDocument{\color{textgray}}

% Create a new command, customparagraph, to have non-bold, gray text
\newcommand{\customparagraph}[1]{{\normalfont #1}}

% Define the dotted line command
\newcommand{\dottedline}{%
  \vspace{0.5em}
  \begin{tikzpicture}
      \draw[dotted, thick, lightgray] (0,0) -- (\textwidth,0);
  \end{tikzpicture}
}


% Define a new command for skill logos
\newcommand{\skilllogo}[2]{
  \begin{minipage}[c]{0.9\linewidth}
    \centering
    \includegraphics[width=0.9\linewidth]{assets/logos/#1.png}
    \vspace{1em}
    \\ \small\textcolor{lightgray}{#2}
  \end{minipage}
}

% Define Spacing of Lists
\setlist[itemize]{itemsep=-1ex, topsep=-1ex, partopsep=1ex, parsep=2ex}

% Define a new command for sections with details
\newcommand{\detailsection}[8]{
    \subsection{#1}
    \vspace{-0.4em}
    \altSection{#2}
    \begin{tabularx}{\textwidth}{@{}XXX@{}}
        {\color{black} \ifblank{#3}{}{#3\: #4}} &
        {\color{black} \ifblank{#5}{}{#5\: #6}} &
        {\color{black} \ifblank{#7}{}{#7\: #8}}
    \end{tabularx}
    \vspace{0.3em}
}

\hypersetup{
  colorlinks = true, %Colors links instead of ugly boxes
  urlcolor = blue, %Color for external hyperlinks
  linkcolor = blue, %Color of internal links
  citecolor = blue, %Color of citations
}

\begin{document}
\pagestyle{empty} % No headers or footers
\small
\setstretch{0.6}
\noindent
\input{information}
\begin{minipage}[t]{0.7\textwidth}
    \vspace{0pt}

    \input{experience}

    \input{projects}

    \input{education}
\end{minipage}
\hfill
\begin{minipage}[t]{0.25\textwidth}
    \vspace{0pt}

    \input{programming-languages}

    \input{technologies}

    \input{certificates}

    \input{languages}
\end{minipage}

\end{document}
