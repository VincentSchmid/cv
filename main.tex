\documentclass{article}
\usepackage{fontspec} % custom fonts
\usepackage{graphicx} % images
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{tikz} % language circles and dotted lines
\usepackage{xcolor} % define colors
\usepackage{setspace}
\usepackage{enumitem} % spacing for list items
\usepackage{fontawesome} % Icons
\usepackage{tabularx}
\usepackage{etoolbox}
\usepackage[left=1.5cm, right=1.5cm, top=1.5cm, bottom=1.5cm]{geometry}

% Define Fonts
\setmainfont[
    Path = ./assets/fonts/Roboto/,  % adjust this path to match your setup
    Extension = .ttf,
    UprightFont = *-Regular,
    ItalicFont = *-Italic,
    BoldFont = *-Bold,
    BoldItalicFont = *-BoldItalic,
]{Roboto}
\newfontfamily\sectionfont[Path=./assets/fonts/]{Panton-BlackCaps.otf}
\newfontfamily\subsectionfont[Path=./assets/fonts/]{Acumin-BdPro.otf}

% Define Colors
\definecolor{altColor}{RGB}{0,157,255}
\definecolor{lightgray}{gray}{0.5}

% Redefine the section formats
\titleformat{\section}
{\Large\sectionfont}
{}
{0pt}
{}
[{\titlerule[1.2pt]}] % Add a line after the section title

% Redefine the subsection format to use \subsectionfont
\titleformat{\subsection}
  {\subsectionfont\fontsize{12}{10}\selectfont\bfseries} % Modify the font settings here
  {} % Label format
  {0pt} % Label separation
  {} % Before code
  [] % After code

% Define the altSection command
\newcommand{\altSection}[1]{%
  {\large\sectionfont\color{altColor} #1\par}%
}

% Define the dotted line command
\newcommand{\dottedline}{%
    \begin{tikzpicture}
        \draw[dotted, thick, lightgray] (0,0) -- (\textwidth,0);
    \end{tikzpicture}%
}

% Define a new command for skill logos
\newcommand{\skilllogo}[1]{\includegraphics[width=0.9\linewidth]{assets/logos/#1.png}}

% Define Spacing of Lists
\setlist[itemize]{itemsep=-1ex, topsep=-1ex, partopsep=1ex, parsep=2ex}

% Define a new command for sections with details
\newcommand{\detailsection}[8]{
    \subsection{#1}
    \altSection{#2}
    \vspace{1em}
    \begin{tabularx}{\textwidth}{@{}XXX@{}}
        \ifblank{#3}{}{#3\: #4} &
        \ifblank{#5}{}{#5\: #6} &
        \ifblank{#7}{}{#7\: #8}
    \end{tabularx}
}

\hypersetup{
  colorlinks = true, %Colors links instead of ugly boxes
  urlcolor = blue, %Color for external hyperlinks
  linkcolor = blue, %Color of internal links
  citecolor = blue, %Color of citations
}

\begin{document}
\small
\setstretch{0.8}
\input{information}
\begin{minipage}[t]{0.65\textwidth}
    \vspace{0pt}

    \input{experience}

    \input{education}
\end{minipage}
\hfill
\begin{minipage}[t]{0.3\textwidth}
    \vspace{0pt}

    \input{skills}

    \input{certificates}

    \input{languages}
\end{minipage}

\end{document}
